{
  "name": "emergency-portal-api",
  "version": "1.0.0",
  "description": "Emergency Response Equipment Portal API",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "func start",
    "dev": "npm run build && func start"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@sendgrid/mail": "^8.1.6",
    "jsonwebtoken": "^9.0.0",
    "jwks-rsa": "^3.0.0",
    "mssql": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
